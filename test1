job('Moodle-test') {
	multiscm {
		 git {
            remote {
                url('git@bitbucket.org:uqam/moodle.git')
                // C'est le id de l'identifiant..
                credentials('b49e52ff-27c6-4d38-b288-dbcd2daef492')
                branches('UQAM_30_DEV')
            }
            extensions {
	            //cleanAfterCheckout()
	            relativeTargetDirectory('moodle')
            }
	    }

	    git {
            remote {
                url('git@bitbucket.org:uqam/auth_saml2.git')
                // C'est le id de l'identifiant..
                credentials('b49e52ff-27c6-4d38-b288-dbcd2daef492')
                branches('UQAM_30_DEV')
            }
            extensions {
	            //cleanAfterCheckout()
	            relativeTargetDirectory('moodle/auth/saml2')
            }
	    }
	}

	steps {
	    // Etape anterieure genere folder @tmp qu'on veut efface.
		shell("find $\{WORKSPACE\} -name '*@tmp' -exec rm -rf {} +; ")
	}
}